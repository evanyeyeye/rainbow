__pycache__/
docs/_build/*
docs/_autosummary/*

.DS_STORE

# local docs build environment
.venv-docs/

# build artifacts
*.egg-info/
build/
dist/
*.so
# Cython-generated C source (regenerated at build time)
rainbow/agilent/_uvdelta.c
rainbow/agilent/_msprofile.c
rainbow/agilent/_chdelta.c

# local benchmark datasets (not part of the repo). The /data/ rule already
# covers every real dataset; do not add dataset-named entries here (the names
# themselves are confidential).
/data/
# exported ASM documents from real samples (never commit; real pharma data)
/ASMs/